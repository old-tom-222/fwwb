启动后端服务：
在back目录下的终端执行：
```
mvn spring-boot:run
```
启动前端服务：
在front目录下的终端执行：
```
npm run serve
```
启动后访问：
http://localhost:8080

本项目使用：
vue3 + spring boot + MongoDB(v6.0.27)

node.js(v22.15.0) + jdk17 + Spring Boot(3.0.2)

需要MongoDB数据库中有fwwb库。